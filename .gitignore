# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# --- Jupyter Notebooks ---
.ipynb_checkpoints/        # Temporary autosave files (your previous error)
*/.ipynb_checkpoints/* # Catch-all for checkpoints in subfolders
.virtual_documents/        # Created by JupyterLab LSP
node_modules/              # If you use Jupyter extensions

# --- Python & Environment ---
__pycache__/               # Compiled Python files
*.py[cod]                  # Compiled bytecode
*$py.class                 # Compiled bytecode
.venv/                     # Virtual environment folder (highly recommended)
venv/                      # Alternative venv name
env/                       # Another common venv name
.env                       # Secret environment variables (API keys, etc.)

# --- Machine Learning Specifics ---
# Data (Ignore large files you don't want on GitHub)
data/raw/                  # Usually you keep raw data local
data/processed/            # Keep processed data local
*.csv                      # Exclude large datasets (unless they are tiny)
*.parquet
*.h5
*.jsonl

# Models (Ignore heavy binary files)
models/*.pkl               # Pickled models
models/*.joblib            # Joblib models
models/*.onnx              # ONNX models
*.pth                      # PyTorch models
*.pb                       # TensorFlow models
checkpoint/                # Model training checkpoints


.DS_Store                  # macOS system files
Thumbs.db                  # Windows system files